{
  "name": "kaleidosound",
  "version": "1.0.0",
  "description": "Kaleidosound — MilkDrop-style audio visualizer for phone and TV",
  "private": true,
  "scripts": {
    "build": "node scripts/build-www.mjs",
    "serve": "node server.js 5173",
    "native:config": "node scripts/apply-native-config.mjs",
    "assets": "node scripts/gen-assets.mjs && npx capacitor-assets generate",
    "sync": "npm run build && npm run native:config && npx cap sync",
    "add:ios": "npx cap add ios && npm run native:config",
    "add:android": "npx cap add android && npm run native:config",
    "open:ios": "npx cap open ios",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0",
    "@capacitor/status-bar": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^7.0.0"
  }
}
